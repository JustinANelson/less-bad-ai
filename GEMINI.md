@./AGENTS.md

# Gemini CLI Notes

- Treat `AGENTS.md` as the canonical shared project guidance.
- Keep this file limited to Gemini CLI integration details; place project rules in `AGENTS.md`.
- Use a nested `GEMINI.md` only when a future package genuinely needs narrower just-in-time context.
- After instruction changes, use `/memory reload`; use `/memory show` to inspect the effective context.
