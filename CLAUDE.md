@AGENTS.md

# Claude Code Notes

- Treat `AGENTS.md` as the canonical shared project guidance.
- Keep this file limited to Claude Code integration details; place project rules in `AGENTS.md`.
- Use a nested `CLAUDE.md` or path-scoped rule only when a future package genuinely needs narrower instructions.
- Use `/memory` to confirm loaded instructions when behavior appears inconsistent.
